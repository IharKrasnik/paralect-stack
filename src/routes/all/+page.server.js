export async function load({ params }) {
	return {
		ogTitle: 'All Startup Stacks — Paralect Stack',
		ogDescription:
			'Discover new tech startup tools and learn how other founders build their startups. Submit your stack!'
	};
}
